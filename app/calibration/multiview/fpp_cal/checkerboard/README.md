# FPP geometry checkerboard bursts

Sixteen **GOOD** HDMI PSP Execute bursts (26 frames each) for camera–projector stereo.
Square pitch **18 mm**, pattern **10×7** inner corners (`bfs_cal/board.yaml`).

| Folder | Role |
|--------|------|
| `00000`–`00012` | Fit (13) |
| `00013`–`00015` | Hold-out (3) |

See `manifest.json` for original `multiview_*` names.

Fit output: `../results/camera_projector_stereo.yaml`.
